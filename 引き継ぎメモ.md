# まめまる 引き継ぎメモ（2026-09-22 の作業ぶん）

次のチャットの最初に、このファイルを渡すか、下の「最初のひとこと」を貼ってください。

---

## 最初のひとこと（これを貼るだけでよい）

> まめまるの続き。`C:\add\claudecode\mamemaru\web` にある CLAUDE.md を読んでから始めて。
> `C:\add\claudecode\mamemaru\android` も使う。引き継ぎメモも渡すので先に読んで。

フォルダは**2つとも**つないでもらう必要があります（`web` と `android`）。

---

## いまの状態 ── ここがいちばん大事

**パソコンの `index.html` がいちばん新しく、GitHub と APK はそれより古いです。**

| どこ | 状態 |
|---|---|
| `web\index.html` | **最新**（344,694 バイト） |
| `android\app\src\main\assets\index.html` | **最新**（同じ中身） |
| GitHub（tenfivetwo/chiisaku-naare） | **古い**。スピーカーの中央ぞろえと `min-width` の修正が未反映 |
| スマホに入っている APK | **古い**。2.3MB 版だが中身は数世代前 |

だから次のチャットで最初にやるのはこの2つです。

1. `web\index.html` と `web\README.md` を GitHub に上げなおす（ドラッグ&ドロップ）
2. Android Studio で APK を作りなおす
   `ビルド` → `Generate App Bundles or APKs` → `Generate APKs`
   （`Generate Signed …` は店に出す用なので押さない）

---

## そのあと、実機でこの6つを確かめる

いちばん上が最優先です。ここが駄目だと作り方から変える話になります。

1. **アプリを完全に閉じて開きなおしても、最高点と顔ぶれが残っているか**
2. ホーム画面の名前が「まめまる」、アイコンがらいおん
3. 上の時計の帯に HUD がかぶっていない
4. 音が鳴る
5. おひるね中に指で玉を動かせる
6. 起動時に白くちらつかず、ピンクのまま立ち上がる

1番は、ゲームを `file://` ではなく `https://mamemaru.localhost/` として読ませている
いちばんの理由です（file:// だと端末によって localStorage が残らない）。

---

## 2026-09-22 にやったこと

- **ストアのスクショ9枚を撮りなおし**（HUD が変わって中身が食いちがっていた）。
  4回まわして1枚ずつ選んだ
- **名前「まめまる」のかぶり調査**。アプリとしては空いている。
  のこるのは商標だけ（J-PlatPat で第9類と第41類）
- **Android アプリ化**。TWA ではなくゲームを同梱する方式。通信の許可なし。
  使っていない部品を外して 6.5MB → 2.3MB
- **にじいろから戻れない**のを直した（タイトルに1行）
- **床下の待機キャラの耳がすける**のを直した
- **HUD の並べかえ**（見送った／スコア／連鎖／次／🔊。Lv⭐ はお題の行の右へ）

くわしい理由は全部 `web\CLAUDE.md` にあります。とくに **設計判断 6-G**（HUD）と
**6-E(14-b)**（耳のすけ）と **設計判断8**（にじいろの出口）。

---

## 手伝う側への申し送り

- **PC に書き込んだら、必ず読みかえして突き合わせること。**
  直前の編集が届かず、最後の1か所だけ落ちることが3回ありました。
  保存先のファイル名を変えると通りました
- **`index.html` は3か所にある。**`web\` を直したら、GitHub と
  `android\app\src\main\assets\` の両方をそろえる。
  忘れるとブラウザ版とアプリ版で中身が食いちがう
- 確認はクラウドの Playwright + Chromium でやる。
  `node test-harness.js`（❌は6件が正常。白っぽい4体の色の近さ）、
  `node tools/monkey.js <seed> <分>`、必要なら使い捨てのスクリプトで実寸を測る
- 作者は非エンジニア。**コマンドを打たせない。** 道具は手伝う側がまわして、
  結果の絵と数字だけを見せる
- 見た目の判断は作者が決める。案を作って**実物を見せてから**選んでもらう
