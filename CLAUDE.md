# まめまる 〜くっつくと ちいさくなる〜

落ちものパズル。**単一HTMLで完結**し、通信を一切しない。ビルド工程なし。

---

## ファイル

| ファイル | 役割 |
|---|---|
| `index.html` | **ゲーム本体。ここが唯一のソース**（HTML・CSS・JSすべて内包、約3000行） |
| `manifest.webmanifest` | PWA設定 |
| `sw.js` | オフライン用 Service Worker |
| `icons/` | アイコン各サイズ（`index.html` の描画コードから生成したもの） |
| `privacy.html` | プライバシーポリシー（両ストアの申請に必須） |
| `.nojekyll` | GitHub Pages の Jekyll 処理を無効化 |

**重複ファイルを作らないこと。** 以前 `mamemaru.html` と `index.html` の2本を手で同期していて事故の温床になったため、1本化した。

---

## ゲームのルール

- 同じ子どうしが触れると合体して**ひとつ小さい子1匹**になる（スイカゲームの逆）
- 順番：しろくま(64) → **うし(49)** → こあら(36) → いぬ(29) → ねこ(26) → ひよこ(22)
- **落とせるのは しろくま〜ねこ の5種**。ひよこは合体でしか生まれない
- ひよこ2匹が合体すると、なかよく横に走って画面外へ去る（＝盤面から2匹減る）
- 上限ラインを超えると**3秒のカウントダウン**、0で終了
- スコア ＝ **みおくり（ひよこ1組=2てん）＋ じかん（10びょう=1てん）＋ 連鎖 ＋ 全消し**

### 顔ぶれ

**「色がかぶらないこと」で決めている。** 現在の割りふり：

| | 色 | 決めた理由 |
|---|---|---|
| しろくま | クリーム | 一番大きい子。もこもこの主役 |
| うし | 白＋黒 | 元は「ぱんだ」→「ぶた（ピンク）」。ぶたは「ピンク＋鼻だけでのっぺりしている」との評価で差し替えた |
| こあら | グレー | 元は「くま」。しろくま・ぱんだ・くまで**熊が3匹**だったため。グレーは空いていた |
| いぬ | 黒＋クリーム | **絵は黒柴**。一度うすい水色にしたが「犬が水色は違和感」で黒柴になった |
| ねこ | 白＋オレンジ＋黒 | **絵は三毛猫**。元はオレンジ一色だった |
| ひよこ | 黄 | |

**表示名と絵は別物。** 「いぬ」は**黒柴**、「ねこ」は**三毛猫**として描いてある。
コード上の識別子（`face:"shiba"` / `ear:"shiba"` / `mike:true`）は絵の内容に合わせてあるので、
表示名だけ変えるときに識別子まで書き換えないこと。

**小さい2匹（ねこ・ひよこ）は 2026-09 に大きくした**（23→26、18→22）。顔が小さすぎて
表情が読めなかったため。「1.5倍」は**いぬを超えてしまうので不可能**だった
（29×1.5=43.5 > こあら36）。いぬを動かさずに入る上限がこの値。
となりとの比は いぬ/ねこ = 1.12 と、いちばん詰まっている。ここをこれ以上詰めると
大きさで段が読めなくなるので、**ねこを 26 より大きくしないこと**（色が違うので 1.12 でも成立している）。

**うしの ぶちは「顔の みぎ 4わり＋その がわの みみ」をまとめて 黒く する。**
ぶちを 3つ 散らした 試作も 作ったが、まとまりが なく 牛に 見えにくかった。
黒の そとがわの ふちは **もこもこの 輪郭を そのまま 計算で なぞっている**
（`popFx` ではなく `drawCreature` の `T.face === "cow"` の中）。丸で 近似すると
ふちに 白い すきまが 出たり 黒が はみ出したり するので、**この計算を 外さないこと**。
内がわの 境目も まっすぐ 切らず、ゆるい なみ線に してある。
右目は 黒の上に 乗るが、白目＋黒目 方式なので 読める（むしろ 牛らしい）。
「1/4だけ 黒」の 案も 作ったが、盤面の r=49 では **黒が 細い帯に なって 輪郭に まぎれた**ので 4わりを 採用。

**ひよこは 2026-09 に 描き足した**（**アホ毛**と **ちいさな はね**）。1.22r の 制約は 守っている。

**うしにして 色は かたよった。** 6匹のうち 色が あるのは ひよこの 黄色だけ。
ぶたの ピンクは 盤面の 彩りとして 効いていた。彩りを 戻したく なったら
**茶白の うし**（`cowA` を `#9A6647` に するだけ）で 両立できる。

**新しい子を足すときは、まず空いている色（みどり・むらさき・みずいろ）から考えること。**
いぬ・ねこ・ひよこが全部うすい黄色で**盤面で見分けられなかった**という失敗が実際にあった。

**耳を あたり判定（半径 r）から 大きく はみ出させないこと。上限は 1.22r。**
このゲームは動物がぎゅうぎゅうに積み上がるので、はみ出した耳が**となりの子の顔にかぶる**。
こあらの試作は最初 1.58r、ぞうの案は 1.68r あって、詰まった盤面が
「動物を詰め込んでいる」ように見えた。こあらは耳の中心を r*.72 まで内側に寄せ、
頭に隠れる部分を増やすことで、見た目の大きさを保ったまま 1.22r に収めてある。

**ぞうは採用しなかった。** 鼻が真下に垂れるため、下に積まれた子と必ず重なる。
こあらは特徴（大きな丸い鼻）が顔の内側で完結するので、密集しても読める。
描画コード（`ear:"zou"` / `face:"zou"`）は残していないので、欲しくなったら描き直しになる。

**うさぎ・ぱんだ・ぶたの描画コードは残してある**
（`ear:"long"` / `sm:"bunny"` / `face:"panda"` / `ear:"pig"` / `face:"pig"` / `sm:"piggy"`）。
`TIERS` に1行足すだけで復活する。ぶたには つむじ と くるんとした しっぽ が 入っている。

**くらい色・もようの上に 目を置かないこと。** ぱんだ（黒いパッチ）と くろしば（黒い顔）で
2回とも「表情がまったく読めない」問題を起こした。対処は共通で、
①目のまわりを明るい色で抜く（黒柴の まろまゆ・マズル、三毛のぶちを目の上に置かない）
②目は**白目＋黒目＋ハイライト**で描く（`T.face === "panda" || T.face === "shiba"` の分岐）
③体のツヤ（`T.hi`）を白から明るい炭色に落とす

### モード（タイトル画面で選択、`mamemaru-spawn` に保存）

**床はどちらのモードでも上がる。** 違いは下からまめまるが増えるかどうかだけ。

| | まめまる ふえない（既定） | まめまる ふえる |
|---|---|---|
| 床の上昇 | 1回につき14px、戻らない | 同じ |
| 下からまめまる | 増えない | 4〜5人の列が入り、盤面が押し上がる |
| 決着（実測・中央値） | ふつう 263秒 | ふつう 178秒 |
| スコア（ふつう） | 128点（みおくり26回） | 95点（みおくり17.5回） |
| 埋まり率 | 29〜31% | 31〜36% |
| **無思考の壁交互AI** | **35点**（うまい86点を大きく下回る） | 126点（うまい143点を下回る） |
| 記録 | `bestFree` | `best` |

**盤面を CH=620 に狭めた後（2026-09）の実測。各セル8回、80回すべて決着した。**
スコアの内訳（ふつう）は **みおくり39 : じかん21 : れんさ28 : おそうじ8**。4項目が
それぞれ効いている。以前は「じかん」が最大級で、実質1変数（生存時間）だった。

---

## 触る前に知っておくべき設計判断

コードから読み取れない「なぜそうなっているか」。**壊さないこと。**

### 1. 放っておくとゲームが終わらない

合体すると小さくなるルールは、盤面が混むほど接触が増えて合体が進む**自動的な安全弁**として働く。実測で、初期版はAIに15分プレイさせても盤面が40〜50匹で安定し一度も詰まなかった。落下速度を上げても、キャラの行を足しても、すべて合体に吸収された。

**「床が戻らずに上がる」を入れて初めて決着する。** 床の上昇は演出ではなく唯一の詰み保証なので、どちらのモードでも必ず動かす。消してはいけない。

### 2. 連鎖は「1回の落下が引き起こした連続合体」

`CHAIN_WINDOW`（0.85秒）内の合体数を数えるが、**次を落とした瞬間に確定させる**（`drop()` の冒頭）。この打ち切りがないと、カーソルを振りながら連打するのが最適解になる（実測で連鎖発生が10回/分 → 4回/分に減った）。

### 3. ひよこの退場は「横へ走る」

上方向に浮かせる案は**昇天に見える**ため却下した。爆発・破裂の表現も、ひよこが弾けているように見えるので不可。粒子は重力で下に落ちる向きにしてある。

**2026-09に見せ方を作り直した。** 以前は等身大・440px/秒で山の中を0.4秒で抜けており、
連続フレームで見ても**座っているひよこと見分けがつかなかった**（評価者の指摘）。
このゲームで一番いい瞬間が、画面上でほぼ不可視だった。現在は：

- **1.85倍**の大きさ、速度 440 → **255px/秒**、寿命 1.2 → **1.15秒**
  （1.75秒にしていた時期があるが、走りきったあと 0.6秒 画面の外で 空回りしていた）
- **山のてっぺんより30px上のレーン**を走る（`popFx` で `top` を求めて `lane` を決める）。
  山の中を走らせると必ず埋もれるので、この計算を外さないこと
- 背後に**やわらかい光の輪**（放射グラデ）と**足もとの影**。光の輪は画面全体を光らせる
  `t:"fl"` とは別物で、光過敏の対象ではない
- **光の輪は「ぜんぶ先に描いてから」体を描く（2パス）。** 1匹ずつ 光→体 の順で描くと、
  うしろの子の光が **前の子の体を白く塗りつぶす**。実際そうなっていて、画面には
  「元気なひよこ1匹＋うしろに黄色いバナナ」しか出ていなかった。内側を べた塗りにする
  `createRadialGradient(...,r*.7, ...)` の 内半径も 0 に して、中心が 白く 潰れないように してある
- **2秒いないに もう1回 起きたら「軽い版」**（`light`）。輪・画面ゆれ・全員びっくり・
  ファンファーレを 省いて 走るだけに する。5秒に1回 フルコースが 来ると、
  一番大事な瞬間が 自分の頻度で 自分を薄める
- 「+2」の てんすう表示を その場に 出す（`t:"txt"`）。得点の40%を かせぐ行為が 無言だった
- 走りながら足もとに砂ぼこりを残す（`stepFx` の `born` 配列。反復中に `fx` へ
  直接 push すると同じフレームで加齢するので、いったん `born` に貯めて後で足している）
- **振り返らせない。** 「元気に駆けていく」＝遊びに行った、と読ませる。
  振り返る案は名残惜しさが出て、人によっては寂しく見えるため採らなかった

### 4. スコアの配分（2026-09に作り直した）

以前は `runBest`（盤面の最大同時数）が柱だった。これは **合体を拒否した量**を測っていたので、
「ひよこを見送るとスコアが下がる」＝**一番かわいい行為が減点**という逆転が起きていた。実測で
「上手いほど あつめた が下がる（29→22）」「わざと合体させないAIが最高得点」を確認して撤廃した。

現在：**みおくり（ひよこ1組=2てん）＋ じかん（10びょう=1てん）＋ 連鎖 ＋ 全消し**。
実測の内訳（ふつう）は **39 : 21 : 28 : 8**。4項目が それぞれ 効いている。

連鎖点 `chainPts(n, spread)` には**よこのひろがり係数**（0.6〜1.0倍）が掛かる。
壁ぎわ1列に積むだけの打ち方が最強になるのを抑えるため。**外さないこと。**

**4れんさ＝3てん から はじまる。** 以前は `ceil(k²/3)` で 4れんさ＝**1てん**だった。
画面じゅうが 虹になって ファンファーレが 鳴るのに みおくり1回（2てん）の 半分で、
**演出の音量と 報酬が さかさま**だった。現在は `ceil((k(k+3)/2 + 1) * w)`。

**ぜんけしの 表示額と 実加算は 必ず そろえる。** `clearAll` は `ceil(n/3)` を 足すのに
表示だけ `ceil(n/2)` のままだった時期があり、「＋6てん」と出て 4点しか 入っていなかった。

### 5. 記録をモード別に分けている

のんびりモードは終わらないためスコアが青天井（実測257点）。同じ枠にすると通常モードの記録が意味を失う。

### 6. 盤面の たかさ CH=620 が 面白さの 生命線（測定済み・最重要）

**CH=710 のとき、狙いの精度は スコアに まったく効いていなかった。**
落とす速さだけ・狙いの精度だけを 別々に 振って 実測した結果：

| 盤面のたかさ | ρ(精度, スコア) | ρ(速さ, スコア) | 埋まり率 |
|---|---|---|---|
| CH=710（前） | **−0.04** | 0.46 | 25〜34% |
| **CH=620（現在）** | **+0.47** | 0.49（0.5秒間隔で頭打ち） | 29〜32% |
| CH=560 | 0.20 | 0.43 | 31〜34% |
| CH=500 | 0.46 | 0.33 | 31〜40% |

CH=710 では 上の8割が いつも 空っぽで、**どこに落としても結果が変わらなかった**。
外部の評価で「連打の速さがスコアを支配している（r=0.76）」と指摘されたのも これが原因で、
盤面が 絶対に 埋まらないので **連打が ノーリスクだった**。620 にすると 連打すると自分が埋まるので、
0.5秒より速く叩くと 逆に スコアが下がる（頭打ち）。**CH を 710 に戻さないこと。**

派生して、**うでまえは「生きた長さ」にしか出ない** という以前の結論も 部分的に 覆った。
ただし以下2つは いまも 有効：

- **床の上昇を「落とした回数」で駆動すると、うでまえが消える**（ρ 0.53 → 0.18）。試して捨てた。時計で動かすこと。
- 短くしすぎると 生存時間の差が 圧縮されて うでまえが 出にくくなる。CH=560 以下は そのぶん ρ が 不安定。

### 7. にじいろ個体は「ひよこだけ」

`SPARKLE_TIER` を4（ねこも光る）にすると、ねこは数が多いので全消しが1ゲーム5回まで増え、
奥の手ではなく日常になる。5（ひよこだけ）に保つこと。かわりに種類が減って対象が薄くなったぶん、
`SPARKLE_RATE` を上げて体感を戻してある（現在 1/28。全消し点を `n/2`→`n/3` に下げた分も込み）。

### 8. 物理

位置ベース（PBD）＋空間グリッド。深いスタックでも最大めり込み1px以内。
**重さは小さい子ほど重い**（1.00〜1.80）。面積ベースだと大きい子が16倍重くなり逆だった。

`MERGE_PAD = 3.5` は必須。ソルバーはめり込みを解消して「ちょうど接する距離」で止めるため、余裕を持たせないと**きれいに接して静止したペアが永久に合体しない**。

---

## 調整用の定数（`index.html` 内）

```js
DROP_POOL     = [0,0,0,1,1,1,2,2,2,3,3,4,4]  // 落とせる種類と比率
CHAIN_MIN     = 4      CHAIN_WINDOW = 0.85    // 連鎖の閾値と判定窓
SPARKLE_RATE  = 1/28   SPARKLE_TIER = 5       // にじいろ個体（合体で全消し）＝ひよこだけ
OVER_GRACE    = 3                             // ライン超過の猶予（秒）
HOLD_LIMIT    = 5.0                           // 落とすまでの制限時間（固定）
RISE_STEP     = 14     RISE_ROW = [0,1,2]     // 床の上昇量／せり上がる種類
LV_SEC        = 30     LV_BACK_MAX = 2        // レベル進行／連鎖でのレベル戻し
CEIL          = 0                             // 天井（合体の押し出しで画面外へ飛ぶのを防ぐ）
CANCEL_UP     = 60                            // 上へスワイプすると落下を取り消せる幅
RISE_BONUS_CAP = 4                            // せり上がり1回あたりの猶予上限
CW=420 CH=620 DANGER=152 HOLD_Y=78            // 盤面（比率固定）★CHは面白さの生命線。設計判断6を読むこと
FLOOR_MIN     = 242                           // ゆかが上がりきるところ
calmFx        localStorage:mamemaru-calm      // 演出ひかえめ（光過敏／reduced-motion）
```

---

## 検証のやり方

ブラウザがなくても、**`<script>` を抜き出してNodeで動かせる**。DOMをスタブして数千フレーム回す方式で、これまで見つけたバグはすべてこれで検出した。

```js
const html = fs.readFileSync('index.html','utf8');
const js = html.match(/<script>([\s\S]*)<\/script>/)[1];
// document / window / localStorage をスタブ、末尾の })(); の前に
// globalThis.__T = { start, update, draw, ... } を注入して eval
```

毎フレーム監視する項目：例外、NaN、壁抜け、床抜け、めり込み、`ctx.save/restore` の不整合、お題が2つあるか、エフェクト配列の暴走。

**モンキーテスト**（ランダム座標の連打、全キーの押しっぱなし、ボタン乱打、画面サイズ変更、dtのばらつき）を8分相当で回すのが標準。実際にこれで「掴んでいる子が壁を突き抜ける」バグを検出した。

---

## 落とし穴

**`sw.js` の `VERSION` はもう上げなくてよい。** `index.html` を **network-first**（まずネット、だめならキャッシュ）に変えたので、差し替えるだけでオンラインの人に新しい版が届く。`VERSION` が効くのはアイコンと manifest だけ。
以前は cache-first で、上げ忘れると**ユーザー側に復旧手段がなかった**（リロードでも何日待っても直らない）。**この構造を cache-first に戻さないこと。**

**タイトル画面は `overlay.innerHTML` で作り直される。** 中の `<canvas>`（`titleCv`・`chainCv`）とボタンは毎回新しい要素になるので、`bindTitleButtons()` で再取得・再初期化している。ここに要素を足すときは同じ扱いが必要。

**全消しと同フレームの合体競合。** `clearedNow` フラグで、全消し直後のそのフレームの合体を止めている。外すと空になった盤面にキャラが生き残る。

**iOSは `user-scalable=no` を無視する。** ダブルタップ拡大・ピンチはJS側（`blockZoom`）で止めている。入力欄とボタンは対象外にしてあるので、ここを雑に触ると記録コードが入力できなくなる。

**天井がある（`CEIL`）。** 合体の押し出しでキャラが画面外へ飛び、実測で**1ゲーム113回**・最長1.8秒も見えなくなっていた。さらにそれが危険判定に引っかかって**カウントダウンが1ゲーム16回誤爆**していた（84%が空中の子）。`resolveWalls()` の天井クランプと、上向き速度の下限 −700 を外さないこと。修正後は飛び出し0回・誤爆0回。

**盤面は420×710の比率固定。** 縦が足りない画面では横も一緒に縮む。iPhone SE3で127×215まで潰れた実績あり。図鑑バーと説明文をタイトル画面の中へ逃がして解決した。ゲーム画面の外に常時表示のUIを足すときは、この縦予算を必ず意識すること。

---

## 電力（スマホのバッテリー）

描画命令を測って削ってある。**むやみに増やさないこと。**

| | 1フレームの canvas 命令数 |
|---|---|
| タイトル画面 | 2620 → **941** |
| ゲーム中（約27人） | 7099 → **4209** |

やったこと：
- **もこもこ輪郭の点数を半径とでこぼこ数から算出**（固定144点 → 24〜96点）。`lineTo` が3611→1824
- **動かない背景（空・にじ・カゴのしましま・みずたま）を裏の画像に焼いて使い回す**。テーマ変更時と床が上がった時だけ焼き直す
- **タイトル中はゲーム画面を6コマ/秒に間引く**（オーバーレイで隠れているため）
- **「つぎ」「あずける」の小さいキャンバスは5コマ/秒**（変化がある時だけ描き直す）
- **devicePixelRatio の上限を2.5→2**（3x端末で画素数36%減）

さらに削るなら、キャラを表情ごとに小さな画像に焼いて貼る方式が残っているが、表情が連続的に変わるので実装は重い。

---

## 次にやる予定（アプリ化）

**第1段（済）** スマホ最適化、セーフエリア、PWA化、アイコン、記録の引き継ぎ機能

**第2段** Capacitorでラップ
- `npx cap init` → `www/` に `web/` の中身を配置 → `npx cap add android`
- **targetSdk は 36**（2026年8月31日以降、Google Play の新規・更新は Android 16 必須）
- iOS は `PrivacyInfo.xcprivacy` が必須。プラグインを増やさなければ申告項目はほぼゼロ
- **Macは持っていない。** Codemagic などのクラウドCIでiOSビルドする方針

**第3段** ストア申請
- Google Play：$25（1回）、**テスター12人×14日のクローズドテストが必須**
- App Store：$99/年。審査員が**機内モードで起動**して確認する（4.2対策）が、本作は完全オフラインなので問題なし
- 両方ともプライバシーポリシーの公開URLが必要

---

## 作者について

非エンジニア。**コマンド操作は前提にせず、ブラウザだけで完結する手順**を提示すること。GitHub Pages への公開もブラウザのドラッグ&ドロップで行っている。公開先は `fivetwo.github.io`。

「かわいさ」と「表現の気持ちよさ」に対する感度が高く、そこは妥協しない。倫理的な引っかかり（動物を詰め込む・弾けさせる等）にも敏感なので、演出を足すときは意味の読まれ方まで考えること。
